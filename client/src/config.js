/**
 * config.js — Re-exports from branding.js for backward compatibility.
 * All existing components that import CURRENCY from here will continue
 * to work. Add new values here as needed.
 */
export { BRAND_CURRENCY as CURRENCY } from './branding'
